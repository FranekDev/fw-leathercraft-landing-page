---
const { title, description, className } = Astro.props;
---
<article class={`${className} border px-4 py-2 rounded-none`} >
    <h3 class="text-xl mb-1 font-(--font-main)">{title}</h3>
    <p class="text-base">{description}</p>
</article>